#!/bin/sh

USER='hugo'

if [ ! -n "$SOURCE" ]; then SOURCE="/app/project/"; fi
if [ ! -n "$BUILD" ]; then BUILD="/app/build/"; fi


function serve() {
    suexec $USER hugo server -p 8080 $SOURCE
}

function build() {
    suexec $USER hugo compile $SOURCE $BUILD
}

function hugo() {
    suexec $USER $@
}

case "$1" in
    serve) serve ;;
    build) build ;;
    hugo) hugo $@ ;;
    *) exec "$@" ;;
esac
